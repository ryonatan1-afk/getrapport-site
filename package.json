{
  "name": "rapport-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-pages.js && node scripts/build-sitemap.js && vite build",
    "build:pages": "node scripts/build-pages.js",
    "build:sitemap": "node scripts/build-sitemap.js",
    "generate": "node scripts/generate-greetings.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@tailwindcss/vite": "^4.2.4",
    "tailwindcss": "^4.2.4",
    "vite": "^8.0.10"
  }
}
